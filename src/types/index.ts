export interface SchoolData {
  rank: number;
  name: string;
  location: string;
  gmat: number;
  classSize: number;
  acceptRate: string;
  fee: string;
  deadline: string;
  tags: string[];
  link: string;
}
